#include "blockchain.h"

/**
 * blockchain_difficulty - computes difficulty to assign to potential Block
 * @blockchain: the Blockchain to analyze
 *
 * Return: difficulty to assign to next Block in Blockchain
*/
uint32_t blockchain_difficulty(blockchain_t const *blockchain)
{
	(void)blockchain;

	return (0);
}
